const fs = require('fs');

const filePath = 'c:\\\\Workspace\\\\aspire-public\\\\src\\\\pages\\\\Programs.jsx';
// Restore the backup in case it was incorrectly modified
// wait, the script truncated the file when it failed because newCardsText was empty?
// Let me verify if file is empty of cards
